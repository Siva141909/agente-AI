name: Deploy Backend → Railway

# Railway auto-deploys via GitHub integration on push to main.
# This workflow only runs checks; actual deploy is handled by Railway's GitHub App.

on:
  push:
    branches: [main]
    paths:
      - "backend_spare/**"
  workflow_dispatch:

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Verify backend files exist
        run: |
          test -f backend_spare/main.py && echo "main.py OK"
          test -f backend_spare/requirements.txt && echo "requirements.txt OK"
          test -f backend_spare/Dockerfile && echo "Dockerfile OK"

# Transaction Parser Requirements
# Install with: pip install -r transaction_parser_requirements.txt

# Core dependencies
torch>=2.0.0
transformers>=4.35.0
accelerate>=0.24.0
sentencepiece>=0.1.99

# Image processing
Pillow>=10.0.0

# Audio processing
librosa>=0.10.0
soundfile>=0.12.0

# Hugging Face Hub (for model downloading)
huggingface-hub>=0.19.0

# API Server (optional, only if using simple_api_server.py)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# Optional but recommended for better performance
# torchaudio>=2.0.0  # Uncomment if you want torchaudio instead of librosa

